#!/usr/bin/python3
def uniq_add(my_list=[]):
    #sum = 0
    #my_list.sort()
    #for i in range(0, len(my_list)):
    #    if(my_list[i] == my_list[i - 1]):
    #        pass
    #    else:
    #        sum += my_list[i]
    return (sum(set(my_list)))
